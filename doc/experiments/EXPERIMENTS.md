# 🧪 实验迭代台账 / Experiment Ledger

> **研究迭代时间线** —— 回答"我试了什么、为什么、学到什么"。
> 与 [`CHANGELOG.md`](../../CHANGELOG.md)（工程里程碑）、[`todo.md`](../../todo.md)（前瞻计划）三层互补，
> 关系见 [`doc/version_tracking.md`](../version_tracking.md)。

## 用法约定

- **一次有目的的训练 / 调参 / 评估 = 一条 `EXP-XXXX`**。ID 在「立项」时分配，贯穿其生命周期（计划 → 进行 → 完成）不变。
- 台账**不复制** SwanLab 已有的指标曲线，只记录工具抓不到的**叙事与决策**（为什么改、结论、下一步），用链接指回 SwanLab run / 训练日志目录 / git commit。
- 来自 `todo.md` 的"下一步"先以 `📋 计划` 立项；执行后**原地回填**结果并改状态，不新建 ID。
- 新建条目推荐用脚本：`python tools/log_run.py --help`（自动拉 git/训练目录/SwanLab 信息，少手填、不漏链接）。
- 单条模板见 [`TEMPLATE.md`](TEMPLATE.md)。
- 状态图例：✅ 完成 · 🔄 进行中 · 📋 计划 · ❌ 放弃/证伪

---

## 索引

<!-- INDEX:HEADER -->
| ID | 日期 | 标题 | 状态 | 一句话结论 |
|----|------|------|------|-----------|
<!-- INDEX:ROWS -->
| [EXP-0004](#exp-0004) | (计划) | 力矩惩罚重训对比 | 📋 计划 | 验证力矩惩罚能否缓解 shank ≥80% 饱和 |
| [EXP-0003](#exp-0003) | (计划) | 指令条件化重训 (obs 45→48) | 📋 计划 | 解锁速度-力矩扫描的前置 |
| [EXP-0002](#exp-0002) | 2026-06-09 | 域随机化冒烟 | ✅ 完成 | 管线不崩，待放量正式训练 |
| [EXP-0001](#exp-0001) | 2026-06-09 | 速度-力矩扫描定论 | ✅ 完成 | 指令未进 obs，策略对 cmd_vx 无响应 |
<!-- INDEX:END -->

---

<!-- ENTRIES:START -->

## EXP-0004
### 力矩惩罚重训对比 — 📋 计划

- **假设**: `reward_scales["torque"] = -2e-4` 起步重训，能缓解 `fl/rl/rr_shank` 的 ≥80% 力矩饱和，且不显著牺牲速度跟踪。
- **改动（计划）**: 启用力矩惩罚（骨架已落地，opt-in）；用 checkpoint 转换器 / 新 ckpt + `eval_plot.py` 对比 `near_limit_frac` / CoT / 功率。
- **依赖**: 最好在 [EXP-0003](#exp-0003)（指令条件化）之后，使扫描可用。
- **关联**: `todo.md` §下一步 #2。
- **结论**: _(待回填)_
- **真机**: _(待回填)_

## EXP-0003
### 指令条件化重训 (obs 45→48) — 📋 计划

- **假设**: 把 `vx/vy/wz` 三维指令加入 observation（45→48 维）后，策略能响应 `--cmd_vx`，从而解锁 [EXP-0001](#exp-0001) 想做的速度-力矩包络扫描。
- **改动（计划）**:
  - 训练侧 obs 45→48（拼入指令）。
  - **同步**改 `deploy/real/rl_deploy.py` 的 `_build_obs` 与 `policy_export.py` 的 `OBS_DIM`（否则 sim2real obs 维度不一致，静默错）。
- **验证**: 重训后 `eval.py --cmd_vx {...}` + `eval_plot.py` 刻画力矩-速度包络，定真机可行速度上限。
- **关联**: `todo.md` §下一步 #1（最高优先级）。由 [EXP-0001](#exp-0001) 的定论催生。
- **结论**: _(待回填)_
- **真机**: _(待回填)_

## EXP-0002
### 域随机化冒烟 — ✅ 完成 (2026-06-09)

- **假设**: 域随机化（DR）EventTerm 接入后，训练管线不崩、能正常起步收敛。
- **改动**: `--domain_rand --obs_noise_std 0.02`，冒烟规模（小 env / 少 iteration）。
- **环境**: env_isaaclab + RTX 5090。
- **commit/tag**: 关联 `v0.5-control-stack`（`f51ee93`）。
- **指标**: SwanLab project=`mos_one-mos` _(补 run 链接)_ ｜ 冒烟通过，管线无报错。
- **结论**: ✅ DR 管线打通。**下一步**：放量正式训练（收敛模型），与无 DR 基线对比抗扰/泛化（推搡、摩擦、±质量）→ 待立项新 EXP。
- **真机**: 未上机。

## EXP-0001
### 速度-力矩扫描定论 — ✅ 完成 (2026-06-09)

- **假设**: 用既有策略在不同 `cmd_vx` 下扫描，可刻画力矩-速度包络、定真机可行速度上限。
- **改动**: 无（用既有 checkpoint + `eval.py --cmd_vx` 扫描）。
- **环境**: env_isaaclab + RTX 5090。
- **commit/tag**: 关联 `v0.5-control-stack`（`f51ee93`）。
- **指标**: 扫不同 `cmd_vx`，关节力矩 / `near_limit_frac` 几乎不变。
- **结论**: ❗**当前策略指令不进 obs（固定单指令训练），对 `--cmd_vx` 完全无响应**，故扫描无意义。
  → 直接催生 [EXP-0003](#exp-0003)（指令条件化重训），把指令加进 obs 后才能做包络扫描。
- **真机**: 未上机。

<!-- ENTRIES:END -->
